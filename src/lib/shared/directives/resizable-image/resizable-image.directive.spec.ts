import { ResizableImageDirective } from './resizable-image.directive';

describe('ResizableImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizableImageDirective();
    expect(directive).toBeTruthy();
  });
});
